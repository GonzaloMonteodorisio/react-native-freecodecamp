import { 
    StyleSheet, 
    StatusBar,
} from "react-native";

const styles = StyleSheet.create({
    container: {
        alignItems: 'center',
    },
    textTheme: {
        fontWeight: 'bold',
    },
});

export default styles;